(() => { const target = document.getElementById('github-repos'); const fallback = document.getElementById('github-fallback'); if (!target) return; const liveSites = { 'MY_Portfolio': 'https://rijan-adhikari.onrender.com', 'proposal-website': 'https://proposal-website-g4n8.onrender.com' }; fetch(`https://api.github.com/users/${SITE_DATA.social.githubUsername}/repos?sort=updated&per_page=6`).then(response => { if (!response.ok) throw new Error('GitHub unavailable'); return response.json(); }).then(repos => { target.innerHTML = repos.map(repo => `<article class="repo"><a class="repo__source" href="${repo.html_url}" target="_blank" rel="noopener"><h3>${repo.name}</h3><p>${repo.description || 'No description yet.'}</p><div class="repo__meta">${repo.language || 'CODE'} · ${repo.stargazers_count} stars</div></a>${liveSites[repo.name] ? `<a class="repo__demo" href="${liveSites[repo.name]}" target="_blank" rel="noopener">View live website <span aria-hidden="true">↗</span></a>` : ''}</article>`).join(''); }).catch(() => { if (fallback) fallback.classList.remove('visually-hidden'); }); })();
