(() => { const target = document.getElementById('github-repos'); const fallback = document.getElementById('github-fallback'); if (!target) return; fetch(`https://api.github.com/users/${SITE_DATA.social.githubUsername}/repos?sort=updated&per_page=6`).then(response => { if (!response.ok) throw new Error('GitHub unavailable'); return response.json(); }).then(repos => { target.innerHTML = repos.map(repo => `<a class="repo" href="${repo.html_url}" target="_blank" rel="noopener"><h3>${repo.name}</h3><p>${repo.description || 'No description yet.'}</p><div class="repo__meta">${repo.language || 'CODE'} · ${repo.stargazers_count} stars</div></a>`).join(''); }).catch(() => { if (fallback) fallback.classList.remove('visually-hidden'); }); })();
