(() => { if (!matchMedia('(pointer:fine)').matches) return; document.querySelectorAll('.magnetic').forEach(button => button.addEventListener('pointermove', event => { const rect = button.getBoundingClientRect(); button.style.transform = `translate(${(event.clientX - rect.left - rect.width / 2) * .08}px, ${(event.clientY - rect.top - rect.height / 2) * .08}px)`; })); document.querySelectorAll('.magnetic').forEach(button => button.addEventListener('pointerleave', () => { button.style.transform = ''; })); })();
