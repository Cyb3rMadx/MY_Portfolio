(() => { const canvas = document.getElementById('hero-canvas'); if (!canvas || !window.THREE) return; let renderer; try { renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true }); } catch (error) { canvas.setAttribute('aria-label', '3D hero visual unavailable in this browser'); return; } const scene = new THREE.Scene(); const camera = new THREE.PerspectiveCamera(42, 1, .1, 100); camera.position.z = 4.8; renderer.setPixelRatio(Math.min(devicePixelRatio, 1.5)); const group = new THREE.Group(); const core = new THREE.Mesh(new THREE.IcosahedronGeometry(1.1, 2), new THREE.MeshBasicMaterial({ color: 0x65e6c2, wireframe: true, transparent: true, opacity: .72 })); const ring = new THREE.Mesh(new THREE.TorusGeometry(1.55, .012, 8, 100), new THREE.MeshBasicMaterial({ color: 0xf2b86b, transparent: true, opacity: .65 })); ring.rotation.x = .8; group.add(core, ring); scene.add(group); const resize = () => { const rect = canvas.getBoundingClientRect(); renderer.setSize(rect.width, rect.height, false); camera.aspect = rect.width / rect.height; camera.updateProjectionMatrix(); }; addEventListener('resize', resize); resize(); const reduce = matchMedia('(prefers-reduced-motion: reduce)').matches; const animate = () => { if (!reduce) { group.rotation.y += .002; group.rotation.x += .001; ring.rotation.z -= .003; } renderer.render(scene, camera); requestAnimationFrame(animate); }; animate(); })();
