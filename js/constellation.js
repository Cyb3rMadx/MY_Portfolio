(() => { const canvas = document.getElementById('skills-canvas'); if (!canvas) return; const ctx = canvas.getContext('2d'); const points = SITE_DATA.skillGroups.flatMap((group, groupIndex) => group.skills.slice(0, 7).map((skill, index) => ({ name: skill.name, x: .12 + ((index * .14 + groupIndex * .11) % .78), y: .15 + ((groupIndex * .17 + index * .09) % .7), color: ['#65e6c2','#f2b86b','#86a8ff','#ff8f8f','#a9d18e'][groupIndex] || '#9aaeb7' }))); const draw = () => { const rect = canvas.getBoundingClientRect(); const scale = devicePixelRatio || 1; canvas.width = rect.width * scale; canvas.height = rect.height * scale; ctx.setTransform(scale, 0, 0, scale, 0, 0); ctx.clearRect(0, 0, rect.width, rect.height); ctx.strokeStyle = 'rgba(159,180,194,.12)'; points.forEach((point, index) => { const next = points[index + 1]; if (next) { ctx.beginPath(); ctx.moveTo(point.x * rect.width, point.y * rect.height); ctx.lineTo(next.x * rect.width, next.y * rect.height); ctx.stroke(); } }); ctx.font = '11px JetBrains Mono, monospace'; points.forEach(point => { const x = point.x * rect.width, y = point.y * rect.height; ctx.fillStyle = point.color; ctx.beginPath(); ctx.arc(x, y, 4, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = '#9aaeb7'; ctx.fillText(point.name, x + 9, y + 4); }); }; addEventListener('resize', draw); draw(); })();
