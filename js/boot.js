(() => { const loader = document.getElementById('loader'); const fill = document.getElementById('loader-fill'); const label = document.getElementById('loader-text'); const lines = ['booting kernel', 'mounting curiosity', 'starting interface', 'system online']; let progress = 0; const timer = setInterval(() => { progress += 20; if (fill) fill.style.width = `${progress}%`; if (label) label.textContent = lines[Math.min(Math.floor(progress / 30), lines.length - 1)]; if (progress >= 100) { clearInterval(timer); setTimeout(() => loader?.classList.add('is-done'), 180); } }, 90); const body = document.getElementById('hero-terminal-body'); if (body) { let i = 0; const tick = () => { if (i < SITE_DATA.identity.heroLines.length) { body.textContent += `${i ? '\n' : ''}${SITE_DATA.identity.heroLines[i++]}`; setTimeout(tick, 360); } }; setTimeout(tick, 550); } })();
