(() => { const dialog = document.getElementById('hidden-terminal'); const input = document.getElementById('hidden-terminal-input'); const log = document.getElementById('hidden-terminal-log'); const close = () => { dialog?.classList.remove('is-open'); if (input) input.value = ''; }; let sequence = []; const konami = ['ArrowUp', 'ArrowUp', 'ArrowDown', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'ArrowLeft', 'ArrowRight', 'b', 'a']; addEventListener('keydown', event => { if (event.key === '`') { dialog?.classList.add('is-open'); input?.focus(); } if (event.key === 'Escape') close(); sequence = [...sequence, event.key].slice(-konami.length); if (sequence.join('|') === konami.join('|')) { document.body.classList.add('konami-unlocked'); log?.insertAdjacentHTML('beforeend', '<div>ACCESS GRANTED: curiosity mode unlocked.</div>'); } }); document.getElementById('hidden-terminal-close')?.addEventListener('click', close); input?.addEventListener('keydown', event => { if (event.key !== 'Enter') return; const command = input.value.trim().toLowerCase(); const output = { help: 'commands: about, skills, clear, exit', about: SITE_DATA.identity.statement, skills: SITE_DATA.skillGroups.flatMap(group => group.skills.map(skill => skill.name)).join(', '), exit: '' }[command] ?? `command not found: ${command}`; if (command === 'clear') log.innerHTML = ''; else if (command === 'exit') close(); else log.insertAdjacentHTML('beforeend', `<div>$ ${command}</div><div>${output}</div>`); input.value = ''; log.scrollTop = log.scrollHeight; }); })();
